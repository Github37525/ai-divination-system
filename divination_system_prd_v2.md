# 卜卦系统（六爻 / 奇门）开发需求文档 (PRD v2.0)

## 1. 文档概述
* **项目名称**：基于确定性规则引擎与 LLM 结合的卜卦/排盘系统（六爻 & 奇门遁甲）
* **版本号**：v2.0（包含硬核逻辑校验、天文历法增强与闭环追踪）
* **核心目标**：建立一套**“规则严密、排盘精准、原典可查、逻辑可控、结果可追溯”**的易学应用架构。通过确定性代码完成所有易学推算与逻辑断言，LLM 仅在严密约束下执行结构化解卦翻译，彻底消除传统 AI 卜卦的伪排盘与幻觉生成。

---

## 2. 系统架构与数据流 (Data Pipeline)

```
[前端用户输入 (含地理位置/经纬度)] 
      │
      ▼
[1. Cast Service (起卦/边界输入校验)] ──(失败)──► [抛出 InvalidInputError 终止]
      │
      ▼
[2. Astronomy & Calendar Service (Swiss Ephemeris / 经纬度真太阳时 / 四柱干支)]
      │
      ▼
[3. Paipan & Focus Engine (装卦 / 纳甲 / 核心“焦点爻”与主次关系提取)]
      │
      ▼
[4. Repository Query (严格读取 JSON 典籍原典卦爻辞)]
      │
      ▼
[5. Guardrails & Rule Assertions (排盘结构校验 + 易学逻辑极值断言)] ──(断言失败)──► [中断流程/熔断]
      │
      ▼
[6. Context Builder & CoT Guard (构建结构化 ai_context + 注入分层推理与合规约束)]
      │
      ▼
[7. LLM Interpretation (三段式 CoT 解读：事实 ➔ 典籍 ➔ 现实映射)]
      │
      ▼
[8. Audit & Feedback Traceability (生成 run_id 落库 + 开放事后“应验度”复盘接口)]
```

---

## 3. 功能模块详细需求

### 模块一：起卦与历法计算模块 (`Cast & Calendar Service`)

#### 1.1 起卦模式与输入校验 (`Cast Service`)
* **数字起卦 (`cast_number`)**：
  * **输入**：3 个 $0–999$ 之间的整数。
  * **校验**：非合法整数或超出范围直接抛出 `InvalidInputError`，拒绝进入排盘。
  * **计算**：上卦 = 数字1 % 8（余0为坤8）；下卦 = 数字2 % 8；动爻 = 数字3 % 6（余0为上爻6）。
* **手摇卦/手动录入 (`cast_manual`)**：
  * **输入**：6 个爻的阴阳及动静状态。
  * **映射转换**：UI 录入顺序（习惯为从上到下）需在后端自动转换为易学标准推算顺序：**初爻（一爻）至上爻（六爻）**（Bottom-to-Top）。

#### 1.2 高精度天文历法与真太阳时 (`Astronomy Service`)
* **输入扩展**：除时间外，增加**地理位置/高精度经纬度（Latitude/Longitude）**输入。
* **计算逻辑**：
  * 引入天文历法算法（如 Swiss Ephemeris 库），基于经度差计算**真太阳时**，解决同平太阳时（如东八区内不同经度）导致的时柱干支与奇门局数排错问题。
  * 节气交接点采用天文时刻毫秒级切分，确保立春、切月时刻绝对精准。
* **干支与神煞计算**：
  * 精确计算年柱、月柱、日柱、时柱。
  * 推算日旬空（`xunkong`）、六神（初爻至上爻排布）、月破、日冲、飞伏神及木库等状态。

---

### 模块二：装卦、焦点提炼与奇门模块 (`Paipan Service`)

#### 2.1 六爻装卦与“焦点爻”自动提取
* **装卦计算**：计算八宫世应、纳甲天干地支、五行生克、六亲（父母/官鬼/妻财/兄弟/子孙）。
* **典籍匹配**：从静态 JSON 数据库中精准读取本卦卦辞、变卦卦辞、动爻爻辞与象辞。**（严格禁止 AI 自行生成或修正原典）**。
* **核心“焦点爻”逻辑提取（新增）**：
  * 代码层根据易学断卦规则（如《卜筮正宗》断卦优先级），自动标记排盘中的核心矛盾：
    * **静卦**：提取世爻与卦辞作为核心。
    * **一爻动**：标记该动爻为核心焦点。
    * **多爻动**：按世爻/持世六亲与动爻生克关系标记主次焦点，并在 Context 中显式声明，引导 AI 集中分析核心矛盾，避免盲目泛化。

#### 2.2 奇门遁甲双入口 (`Qimen Service`)
* **问事奇门**：针对单项事务，输入占问类别与即时时间，生成九宫盘（天盘、地盘、人门、神盘、星宿、局数）。
* **终身盘**：结合出生地经纬度换算真太阳时，生成四柱、大运宫位/起运年龄（如 4.2–14.2 岁）、流年流月运势及五行能量得局得分表。

---

### 模块三：Guardrails 与易学逻辑断言检查 (`Guardrails Engine`)

在数据交由 LLM 生成解答前，必须通过两层断言检查，触发任意错误即熔断（`Stop with error`）：

1. **结构完整性校验**：
   * 6 个爻位数据完整，无空值或异常位。
   * 本卦与变卦（若有动爻）映射关系完整。
   * 静态 JSON 中的卦辞、爻辞匹配成功且无缺失。
2. **易学逻辑极值断言（新增）**：
   * **静卦断言**：若动爻数为 0，断言变卦必须为空，防止出现伪变卦。
   * **六爻皆动/全变断言**：触发特殊用九/用六或乾坤两卦特例校验。
   * **冲合矛盾断言**：对月破逢空、暗动与日破的临界状态进行硬逻辑标记，确保给到 AI 的数据不存在规则层面的矛盾描述。

---

### 模块四：Context 构建、CoT 约束与 LLM 解读 (`LLM Service`)

#### 4.1 结构化 Context 与思维链 (CoT) 约束
* **Context 构建**：仅向 LLM 传入经过校验的结构化 JSON（包含：用户问题、排盘客观事实、代码提炼的焦点爻、匹配好的原典文本）。
* **三段式 Prompt 约束（Chain of Thought）**：
  强制 LLM 按以下逻辑链条输出解卦文本，禁止跳跃拟合：
  1. **第一阶段（盘面事实）**：客观陈述排盘结构（如：世爻位置、焦点爻生克关系、旬空状态）。
  2. **第二阶段（典籍语义）**：解释原典卦辞、爻辞在经典语境中的本意。
  3. **第三阶段（现实映射）**：将前两阶段的客观推演映射至用户占问的实际问题。

#### 4.2 合规与风险防护栏 (Compliance Guardrails)
* **禁止确定性决策**：系统与 Prompt 层强制注入合规约束，禁止对医疗诊断、法律诉讼、金融买卖、生死断言给出 100% 确定性的操作指令。
* **引导式免责声明**：AI 输出末尾需统一附带合规提示：“本解读基于传统易学典籍与逻辑模型推演，仅供文化体验与决策参考”。

---

### 模块五：审计追溯与事后应验闭环 (`Audit & Feedback Loop`)

#### 5.1 全链路追踪 (`run_id`)
* 每次运行生成唯一 `run_id`（UUID）。
* 全链路落库记录：`run_id` ➔ 原始输入（含经纬度） ➔ 天文/干支推算结果 ➔ 焦点爻提取结果 ➔ 校验日志 ➔ 传给 LLM 的 Context ➔ LLM 最终输出。

#### 5.2 事后应验度反馈机制（新增）
* **反馈接口 (`feedback_api`)**：在历史记录模块为用户开放“事后验证”功能。
* **数据维度**：
  * 应验状态：`应验` / `未应验` / `部分应验` / `尚无结果`。
  * 用户补充说明：实际发生事件与时间的简述。
* **闭环价值**：反馈数据与 `run_id` 绑落库，用于后续评估 Prompt 解卦质量，并作为后续微调（Fine-Tuning）专属易学解卦大模型的高质量数据集。

---

## 4. UI / UX 界面需求调整

1. **地理位置输入组件**：在数字卦/奇门终身盘输入区域，增加“出生地/占问地”选择器（支持自动获取 GPS 经纬度或手动精确选择县级市）。
2. **盘面焦点高亮**：在排盘主界面，对代码层提取出的“核心焦点爻”给予视觉上的强高亮（如金色边框或专属标签），使用户与 AI 解读的重心保持一致。
3. **调试与追溯面板**：
   * 展开“调试轨迹（`run_id`）”时，除了展示 UUID，还可查看当前局象的 Guardrails 校验通过日志。
4. **历史记录与复盘卡片**：历史列表中增加“标记应验”快捷入口，支持用户在未来特定时间节点对该卦象进行打卡反馈。

---

## 5. 关键技术指标与风险控制

| 维度 | 指标 / 风险点 | 控制策略 |
| :--- | :--- | :--- |
| **计算时延** | 确定性排盘 + 天文计算 | 耗时控制在 $150\text{ms}$ 以内 |
| **历法精度** | 节气与真太阳时偏差 | 引入 Swiss Ephemeris 算法库，经纬度偏差控制在县级以内 |
| **AI 幻觉率** | 原典错乱 / 卦名伪造 | 100% 阻断，原典由 JSON 硬注入，LLM 仅允许做文本翻译 |
| **合规风险** | 平台政策 / 违规断言 | 引入关键词过滤与 Prompt 三段式 CoT 约束，屏蔽绝对化决策 |
